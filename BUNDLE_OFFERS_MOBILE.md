# عروض الـ Bundle ("اشتري X واحصل على Y مجانًا") — تطبيق العميل (الموبايل)

> آخر فيتشر اتأكد منها حي: العميل يقدر يختار **مقاس مختلف لكل قطعة** في العرض — سواء القطع المدفوعة أو قطعة الهدية — مش لازم يكونوا كلهم نفس المقاس.

## الفكرة

العرض (Bundle) بيتحدد بيه: كام قطعة "مدفوعة" (`requiredPaidQuantity`) وكام قطعة "هدية" (`freeQuantity`). مثال: "٢ مدفوع + ١ هدية".

كل قطعة (مدفوعة أو هدية) هي سطر مستقل له `serviceId` + `sizeId` (اختياري) + `quantity` الخاصة بيه — يعني تقدر توزّع القطعتين المدفوعتين على مقاسين مختلفين تمامًا، وتحدد مقاس الهدية بنفسك برضو.

## الإندبوينت

### `POST /orders/calculate/order` (معاينة السعر) أو `POST /orders` (تأكيد الطلب)

```json
{
  "branchId": 1,
  "addressId": 1,
  "bundleSelections": [
    {
      "bundleId": 7,
      "paidItems": [
        { "serviceId": 1, "sizeId": 1, "quantity": 1 },
        { "serviceId": 1, "sizeId": 2, "quantity": 1 }
      ],
      "freeItems": [
        { "serviceId": 1, "sizeId": 2, "quantity": 1 }
      ]
    }
  ],
  "paymentMethod": "CASH"
}
```

- `paidItems` / `freeItems`: مصفوفة أسطر — كل سطر `{ serviceId, sizeId?, addonIds?, quantity }`.
- **مجموع `quantity` في `paidItems` لازم يساوي `requiredPaidQuantity` بتاع العرض بالظبط**، ومجموع `quantity` في `freeItems` لازم يساوي `freeQuantity` — مش شرط سطر واحد بكمية، ممكن توزّعها على كذا سطر بمقاسات مختلفة (زي المثال فوق: سطرين بكمية 1 لكل واحد بدل سطر واحد بكمية 2).
- `sizeId` اختياري في كل سطر — لو المنتج مالوش مقاسات، سيبه فاضي.

## الرد (من `calculate` أو داخل تفاصيل الطلب)

كل قطعة بترجع بتفاصيلها الكاملة (السعر، المقاس، هل هي مجانية):

```json
{
  "serviceId": 1, "sizeId": 2, "quantity": 1,
  "selected": { "size": { "id": 2, "name": {"ar":"كبير","en":"Large"}, "price": 90, "priceAfterDiscount": 75 } },
  "isFree": true,
  "itemTotalPrice": 0,
  "originalBaseValue": 75
}
```

`isFree: true` → `itemTotalPrice` دايمًا صفر مهما كان سعر المقاس — القيمة الأصلية (`originalBaseValue`) بترجع للعرض بس (زي "كنت هتدفع فيها ٧٥ جنيه، هدية").

## ⚠️ حالة مهمة لازم تتعامل معاها في الواجهة: رفض اختيار مقاس معيّن للهدية

كل عرض معاه قاعدة اسمها `freeValueRule` بتحدد **أعلى قيمة مسموح بيها لقطعة الهدية**:

| القيمة | يعني إيه |
|---|---|
| `NO_CAP` | أي مقاس مسموح للهدية، حتى لو أغلى من المدفوع |
| `CAP_TO_CHEAPEST_PAID` (الشائع/الافتراضي) | الهدية لازم متتعديش سعر **أرخص قطعة دفعها العميل** في نفس الاختيار |
| `MAX_FREE_VALUE` | فيه سقف سعر ثابت للهدية (`maxFreeItemValue`) بغض النظر عن المدفوع |

لو العميل اختار مقاس هدية أغلى من المسموح، الـ API بيرجّع **400** برسالة:
```json
{ "message": "Free item value exceeds the bundle limit" }
```

**التوصية للواجهة:** لما تعرض خيارات مقاس الهدية للعميل، لو العرض شغّال بقاعدة `CAP_TO_CHEAPEST_PAID`، فلتر المقاسات المعروضة للهدية عشان تفضل بس المقاسات اللي سعرها ≤ أرخص مقاس اختاره العميل في الجزء المدفوع — كده تتجنب إنه يختار ويترفض بعد كده. تقدر تجيب قيمة `freeValueRule` و`maxFreeItemValue` من بيانات العرض نفسه (`GET /bundles/:id`).

## قاعدة تانية: لازم المقاس يكون بالاسم المطلوب أحيانًا

لو `paidSizeRule` أو `freeSizeRule` = `NAME` (بدل `ANY`)، يبقى فيه اسم مقاس محدد إجباري (`paidRequiredSizeName`/`freeRequiredSizeName`، زي "Large" بالظبط) — لو العميل اختار مقاس باسم مختلف، الطلب يترفض بـ:
```json
{ "message": "Selected size does not match the bundle rule" }
```
**التوصية:** لو القاعدة `NAME`، منعّاش خيارات تانية في الواجهة أصلاً — اعرض المقاس المطلوب بس (أو اخفي اختيار المقاس تمامًا لو مفيش غيره متاح).

## اتأكدنا منه حي

عملنا عرض حقيقي "٢ مدفوع + ١ هدية"، طلبنا مقاس صغير + مقاس كبير كمدفوع، ومقاس كبير كهدية (بقاعدة `NO_CAP`) — رجع السعر صح: الاتنين المدفوعين اتحسبوا بسعرهم الحقيقي، والهدية اتحسبت بصفر جنيه بالظبط. نضّفنا بيانات الاختبار بعدها.
