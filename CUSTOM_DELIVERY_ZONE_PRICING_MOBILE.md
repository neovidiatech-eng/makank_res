# تسعير المناطق للمندوب الخاص + التوصيل العادي — تطبيق العميل (الموبايل)

> آخر فيتشرز اتأكد منها حي: (١) محطات المندوب الخاص (مشتريات/مطاعم/أونلاين) بقت **المنطقة إجبارية والموقع اختياري** بدل العكس. (٢) لو العميل مختار منطقة من الدروب منيو، السعر بياخد المنطقة دي بالأولوية دايمًا — سواء في التوصيل العادي أو المندوب الخاص.

## الفكرة العامة

فيه ٣ أنواع مندوب خاص بيشتغلوا كلهم من نفس حوض المناديب:

| `kind` | الاستخدام | الاندبوينت |
|---|---|---|
| `PURCHASE` (افتراضي) | مندوب مشتريات — "روح اشتريلي كذا" | `POST /orders/custom-delivery` |
| `RESTAURANT` | نفس آلية `PURCHASE` بالظبط، بس تسميات مختلفة في الواجهة (محطة → مطعم) | `POST /orders/custom-delivery` مع `"kind": "RESTAURANT"` |
| `ONLINE` | مرسل واحد (بائع أونلاين) + دفعة طلبات لمستلمين مختلفين مرة واحدة | `POST /orders/online-delivery` (اندبوينت منفصل تمامًا) |

الفرق الوحيد بين `PURCHASE` و`RESTAURANT` هو القيمة اللي بترجع في `customDeliveryKind` — استخدمها في الواجهة بس لتغيير التسميات (محطة/مطعم، قائمة مشتريات/قائمة أوردر). المنطق والتسعير والتعيين كلهم واحد.

### كل الاندبوينتس + هل محتاجة تسجيل دخول؟

| الاندبوينت | Method | تسجيل دخول؟ |
|---|---|---|
| `/orders/custom-delivery/calculate` | POST | **لأ** — زائر (visitor) مسموح، مفيد لمعاينة السعر قبل ما يعمل حساب |
| `/orders/custom-delivery` | POST | **أيوه** إجباري |
| `/orders/custom-delivery/images` | POST | **أيوه** إجباري |
| `/orders/custom-delivery/:id/advance` | PATCH | **أيوه** إجباري (المندوب بس) |
| `/orders/custom-delivery/:id/finish` | PATCH | **أيوه** إجباري (المندوب بس) |
| `/orders/online-delivery/seller-profile` | GET | **أيوه** إجباري |
| `/orders/online-delivery/calculate` | POST | **أيوه** إجباري (على عكس المشتريات/المطاعم، هنا مفيش معاينة بدون تسجيل دخول) |
| `/orders/online-delivery` | POST | **أيوه** إجباري |
| `/stores/:id/effective-zone-prices` | GET | **لأ** — زائر مسموح |

---

## ١. محطات المندوب الخاص (Purchase/Restaurant): المنطقة إجبارية، الموقع اختياري

### قبل كده
```json
{ "lat": 24.7136, "lng": 46.6753, "label": "المستودع" }
```
كان `lat`/`lng` إجباريين و`zoneId` اختياري.

### دلوقتي (المطلوب من الموبايل)
```json
{ "zoneId": 5, "label": "المستودع" }
```
- **`zoneId` بقى إجباري** على كل محطة (استلام أو تسليم).
- **`lat`/`lng` بقوا اختياريين**:
  - لو العميل بيحدد بينك على الخريطة، ابعتهم عادي — هيتسجلوا كموقع حقيقي للمحطة (بيستخدموا في حساب المسافة والتغطية الجغرافية).
  - لو مفيش بينك (اختيار منطقة بس من الدروب منيو، زي الأونلاين بالظبط)، متبعتش `lat`/`lng` خالص — الباك إند هيحسبهم تلقائي من مركز المنطقة (centroid) عشان يفضل كل حاجة شغالة (التسعير، التحقق من التغطية) من غير ما تحتاج تبعت إحداثيات وهمية.

الشكل الكامل للمحطة (كل الحقول ما عدا `zoneId` اختيارية):
```json
{
  "zoneId": 5,
  "lat": 24.7136,
  "lng": 46.6753,
  "label": "المستودع",
  "name": "ورشة النور",
  "purchaseList": "كيلو موز + علبة عصير",
  "estimatedCost": 50,
  "notes": "اطلب الصنف الكبير",
  "imageIds": [12, 13]
}
```

### ⚠️ مهم جدًا — مندوب المشتريات/المطاعم **مفيهوش بيانات "مستلم" خالص**

راجعنا الموضوع مع بعض وأكدنا: مندوب المشتريات (والمطاعم) مفهومه الأصلي إن العميل بيروح لمحطة/محطات مختلفة
يشتري منها حاجات، مش إنه بيبعت طلب لمستلم معين. عشان كده **مفيش حقل اسم مستلم أو رقم تليفون مستلم خالص في
بيانات المحطة** — كل محطة هي بس: **مكان (`zoneId`/`label`/`name`) + قائمة اللي عايزه منه (`purchaseList`)**،
سواء كانت أول محطة أو آخر محطة أو أي محطة وسطى. لو الفرونت حاليًا بيعرض خانة "اسم/رقم المستلم" في شاشة إضافة
محطة لمندوب المشتريات أو المطاعم، **دي مش موجودة في الباك إند وميحتاجش يبعتها** — لازم تتشال من الواجهة.

(الاستثناء الوحيد اللي فيه بيانات "مستلم" حقيقية هو مندوب **الأونلاين** بس — لأنه أصلاً مبني على فكرة مختلفة:
بائع واحد بيبعت لمستلمين مختلفين. شوف القسم التالت.)

### كل محطة (غير الأخيرة) = استلام تلقائيًا، بغض النظر عن ترتيب الإضافة

الباك إند بيحدد نوع كل محطة تلقائي حسب **الترتيب في المصفوفة**، مش حسب وقت الإضافة: **أي محطة غير الأخيرة =
استلام (Pickup)**، **والمحطة الأخيرة بس = تسليم (Dropoff)**. يعني لو العميل ضاف محطة تالتة أو رابعة (زي ما هو
موصوف: "عايز حاجة من مكان، بعدين يضيف محطة تانية، بعدين تالتة")، أي محطة يضيفها **قبل** المحطة الأخيرة هتترجم
تلقائي كـ"استلام" — الباك إند مش محتاج أي تعديل هنا، الموضوع شغال صح من الأول. لو الفرونت حاليًا بيعرض نموذج/
تسميات "تسليم" لمحطة جديدة متضافة، **دي مشكلة في الفرونت (القالب/التسميات الغلط بتتعرض)** مش نقص في الباك إند —
لازم تتصلح في الموبايل بس.

### الاندبوينتس اللي بتاخد `stops[]`
- `POST /orders/custom-delivery/calculate` — معاينة السعر (visitor مسموح)
- `POST /orders/custom-delivery` — تأكيد الطلب (لازم تسجيل دخول)

كلاهما بياخد نفس شكل `stops` بالظبط (أول عنصر = نقطة الانطلاق، آخر عنصر = الوجهة، وبينهم محطات وسطى اختيارية — الحد الأدنى محطتين).

**الرد** (من `calculate` أو من `data` في تأكيد الطلب) بيرجّع الـ`stops` بعد ما الباك إند يكمّلهم — يعني لو بعتّ محطة من غير `lat`/`lng`، هترجعلك جوه الرد وهي متملية بمركز المنطقة:
```json
{
  "stops": [
    { "zoneId": 1, "label": "Pickup", "lat": 30.045, "lng": 31.235 },
    { "zoneId": 2, "label": "Dropoff", "lat": 26.4399, "lng": 50.108 }
  ],
  "estimatedItemsCost": 50,
  "globalCommission": 5,
  "extraStopFee": 0,
  "adminCommission": 5,
  "shipping": 60,
  "tip": 0,
  "deliveryDiscount": 0,
  "total": 115
}
```

### حقول تانية على مستوى الأوردر (كلها اختيارية غير `paymentMethod`)

| الحقل | النوع | ملاحظة |
|---|---|---|
| `kind` | `"PURCHASE"` \| `"RESTAURANT"` | افتراضي `PURCHASE` لو متبعتش |
| `paymentMethod` | enum | **إجباري** — شوف قسم "طريقة الدفع" تحت |
| `paidWithWallet` | boolean | شوف قسم "طريقة الدفع" |
| `isGift` | boolean | افتراضي `false` |
| `note` | نص | ملاحظة عامة على الأوردر كله (مش لكل محطة) |
| `tip` | رقم | إكرامية اختيارية |
| `itemsDescription` / `estimatedItemsCost` | نص / رقم | وصف عام وتكلفة تقديرية للطلب كله — مستخدمة بس لو المحطات نفسها من غير `estimatedCost` خاص بيها، وإلا بيتاخد مجموع تكاليف المحطات |
| `driverInstructions` | نص | تعليمات للمندوب (مش نفس ملاحظة المحطة `notes`) |
| `fortuneRewardId` | رقم | معرّف جائزة عجلة الحظ — **نوع `FREE_DELIVERY` بس مقبول هنا** |

### أخطاء ممكن تواجهها (رسايل الـ400 بالظبط)

| الحالة | الرسالة |
|---|---|
| أقل من محطتين | `"يجب إدخال مكانين على الأقل (بداية ونهاية)"` |
| الخدمة مقفولة من الإعدادات | `"خدمة المندوب الخاص مغلقة حالياً"` |
| محطة برة نطاق تغطية أي منطقة فعّالة | `"Pickup location is outside our service zones"` (لو أول محطة) أو `"Delivery destination is outside our service zones"` (لو آخر محطة) |
| أكتر من ٥ صور لمحطة واحدة | `"لا يمكن إرفاق أكثر من 5 صور لكل محطة"` |
| أكتر من ٢٠ صورة في كل الطلب | `"لا يمكن إرفاق أكثر من 20 صورة للطلب الواحد"` |
| صورة متكررة في نفس الطلب | `"لا يمكن استخدام نفس الصورة أكثر من مرة"` |
| صورة مش بتاعة اليوزر ده أو مستخدمة قبل كده | `"بعض الصور غير صالحة أو مستخدمة من قبل"` |

---

## ٢. التسعير: المنطقة أولاً، بعدين الحساب العادي

### توصيل المندوب الخاص (Purchase/Restaurant/Online)
لكل محطة **تسليم** (آخر محطة، أو كل محطة `DROPOFF` في حالة الأونلاين):
1. لو المنطقة (`zoneId`) عندها سعر توصيل ثابت محدد من الأدمن → يتاخد هو.
2. لو مفيش سعر محدد للمنطقة → الحساب يرجع للطريقة العادية (كيلومتر × سعر الكيلو + عمولة ثابتة من الإعدادات)، باستخدام الموقع الحقيقي أو مركز المنطقة (لو الموقع متبعتش).

مفيش حاجة إضافية مطلوبة من الموبايل هنا — نفس البودي اللي فوق كفاية، السعر بيتحسب صح تلقائي.

### التوصيل العادي (أوردر من متجر/مطعم)
نفس المبدأ، لكن فيه أولوية إضافية مهمة اتغيرت:

> **قبل كده**: المنطقة اللي بتتحسب منها السعر كانت منطقة العنوان الحقيقية (GPS/lat-lng بتاع العنوان).
> **دلوقتي**: **المنطقة اللي العميل بيختارها من الدروب منيو (`zoneId` في `POST /orders` أو `/calculate/order`) بتاخد الأولوية دايمًا**، حتى لو مختلفة عن منطقة عنوانه الحقيقية.

الترتيب بالظبط:
1. المنطقة المختارة (`zoneId`) عندها سعر خاص بالمتجر ده تحديدًا؟ → يتاخد هو.
2. المنطقة المختارة عندها سعر عام (مش خاص بمتجر)؟ → يتاخد هو.
3. لو المنطقة المختارة مالهاش سعر خالص (ولا خاص ولا عام) → يرجع الحساب لمنطقة العنوان الحقيقية (نفس المنطق: سعر خاص بالمتجر → سعر عام → كيلومتر).
4. لو العميل مبعتش `zoneId` خالص → زي ما هو، يحسب على منطقة العنوان الحقيقية مباشرة.

**العنوان (`addressId`) يفضل هو مصدر الحقيقة للتغطية الجغرافية** (هل المتجر بيوصل للمنطقة دي أصلاً ولا لأ) — الاختيار من الدروب منيو بيأثر بس على *السعر*، مش على التغطية.

#### طلب `zoneId` مع الأوردر العادي
```json
{
  "branchId": 1,
  "addressId": 7,
  "zoneId": 5,
  "items": [...]
}
```
`zoneId` هنا اختياري تمامًا زي ما هو — لو العميل مختارش منطقة من الدروب منيو، سيبه فاضي وهيشتغل زي الأول بالظبط.

#### إزاي تجيب قائمة أسعار المناطق الخاصة بمتجر معين
عشان تعرض السعر الصح لكل منطقة في الدروب منيو وانت لسه في شاشة المتجر (قبل ما تعرف عنوان العميل حتى)، استخدم:

```
GET /stores/:id/effective-zone-prices
```
(متاح لأي حد، حتى بدون تسجيل دخول — مناسب لشاشة استعراض المتجر)

الرد:
```json
[
  { "zoneId": 1, "name": { "ar": "وسط القاهرة", "en": "Central Cairo" }, "cityId": 1, "price": 45 },
  { "zoneId": 2, "name": { "ar": "منطقة 2", "en": "Zone 2" }, "cityId": 1, "price": null }
]
```
`price: null` يعني المتجر ده مالوش سعر خاص ولا فيه سعر عام لهذه المنطقة — لو العميل اختارها، السعر النهائي هيتحسب بالكيلومتر وقت تأكيد الطلب. **متعتمدش على `GET /zones` العادي لعرض أسعار المتجر** — ده بيرجّع المناطق نفسها بس من غير أسعار المتجر، مش المفروض تستخدمه لعرض السعر المتوقع للعميل.

---

## ٣. المندوب الأونلاين (`ONLINE`) — دفعة طلبات لمستلمين مختلفين

اندبوينت منفصل تمامًا (مش نفس `stops[]` بتاع Purchase/Restaurant):

```
POST /orders/online-delivery/calculate   ← معاينة
POST /orders/online-delivery             ← تأكيد
GET  /orders/online-delivery/seller-profile   ← بيانات البائع المحفوظة (اسم/تليفون/منطقة استلام) لو موجودة
```

```json
{
  "isOnlineSeller": true,
  "senderName": "متجر ندى",
  "senderPhone": "01012345678",
  "pickupZoneId": 1,
  "pickupLat": 30.035,
  "pickupLng": 31.225,
  "recipients": [
    {
      "recipientName": "أحمد علي",
      "recipientPhone": "01098765432",
      "deliveryZoneId": 5,
      "addressDetails": "شارع النصر، عمارة 12، الدور 3، شقة 5",
      "lat": 30.065,
      "lng": 31.255,
      "itemsDescription": "فستان مقاس M",
      "estimatedCost": 300,
      "collectionAmount": 300,
      "packagingRequested": false,
      "notes": "برجاء الاتصال قبل الوصول",
      "imageIds": [12, 13]
    }
  ],
  "paymentMethod": "CASH"
}
```
- `senderName`/`senderPhone`/`pickupZoneId` اختياريين لو البائع عنده بروفايل محفوظ بالفعل (`GET /online-delivery/seller-profile`) — لو أول مرة، لازم تتبعت.
- `recipients`: مصفوفة، عنصر واحد على الأقل — كل عنصر بيبقى محطة تسليم مستقلة بمنطقتها وعنوانها.
- **`pickupLat`/`pickupLng` (جديد) و`lat`/`lng` لكل مستلم (جديد)**: زرار "خريطة" اختياري جنب العنوان — زي بالظبط
  مندوب المشتريات/المطاعم. **المنطقة (`pickupZoneId`/`deliveryZoneId`) تفضل إجبارية دايمًا**، والخريطة بس زيادة
  اختيارية فوقها. شوف قسم التسعير تحت لإزاي بتأثر على السعر.
- **`imageIds`**: نفس آلية الرفع بتاعة مندوب المشتريات/المطاعم بالظبط — ارفع الصور الأول عبر
  `POST /orders/custom-delivery/images` (بترجع `imageIds`)، وابعتهم هنا في `imageIds` بتاع كل مستلم على حدة —
  كل مستلم صوره الخاصة بيه بس. اختياري، وحده أقصى ٥ صور لكل مستلم.

### التسعير — ٣ مستويات بالظبط (زي المشتريات/المطاعم تمامًا)

1. **لو منطقة أول مستلم في القايمة عندها سعر توصيل ثابت محدد من الأدمن** → يتاخد هو، حتى لو الخريطة مستخدمة.
2. **لو مفيش سعر منطقة، لكن البائع واختار الخريطة (`pickupLat`/`pickupLng`) وأول مستلم كمان اختار الخريطة
   (`lat`/`lng`)** → السعر يتحسب **بالمسافة الحقيقية بالكيلومتر** بين النقطتين (نفس معادلة مندوب
   المشتريات/المطاعم بالظبط). **لو حد من الاتنين بس مستخدم الخريطة (مش الاتنين مع بعض)، مبيتحسبش بالكيلومتر —
   يرجع للبند ٣.**
3. **لو مفيش سعر منطقة ومفيش خريطة (أو خريطة ناقصة من طرف واحد)** → رسم أساسي ثابت من الإعدادات
   (`onlineRepresentativeBaseFee`).

**كل مستلم زيادة بعد الأول** (تاني/تالت مستلم...) بياخد رسم "محطة إضافية" ثابت من الإعدادات
(`onlineRepresentativeExtraStopPrice`) — **دايمًا ثابت بغض النظر عن منطقته أو الخريطة بتاعته هو**، حتى لو هو
كمان استخدم زرار الخريطة. الخريطة بتأثر بس على سعر أول مستلم في القايمة.

**الشحن الكلي = سعر المستلم الأول (حسب البنود التلاتة فوق) + (عدد المستلمين الزيادة × رسم المحطة الإضافية)**.

مثال: مستلمين، البائع وأول مستلم استخدموا الخريطة والمسافة الحقيقية بينهم طلعت 10.82 جنيه، ورسم المحطة الإضافية
15 جنيه → الإجمالي = 10.82 + 15 = 25.82 جنيه (+ العمولة). اتأكد من المثال ده حي بالظبط.

**ملحوظة لمرحلة الإطلاق الحالية**: كل مناطق المندوب الخاص بأنواعه التلاتة (Purchase/Restaurant/Online) هتفضل من
غير سعر محدد في البداية — يعني عمليًا البند (١) مش هيتفعّل دلوقتي، والتسعير هيعتمد على البند (٢) لو العميل
استخدم الخريطة، أو البند (٣) (الإعدادات الثابتة) لو لأ.

---

## ⚠️ طريقة الدفع — `paymentMethod` غير `paidWithWallet`، ومهم متتلخبطش بينهم

دي حتة سهل جدًا تتلخبط فيها لو بنيت واجهة دفع واحدة على كل الأنواع من غير ما تفرّق. فيه **حقلين مختلفين
تمامًا**:

### `paymentMethod` (إجباري) — إيه القيمة: `CASH` أو `WALLET`
- **`CASH`**: العميل هيدفع كاش للمندوب وقت الاستلام. مفيش أي حقل إضافي مطلوب.
- **`WALLET`** (الاسم ده مضلل — **مش معناها الدفع من محفظة التطبيق**، معناها "تحويل بنكي/فودافون كاش/إنستاباي
  مع صورة إيصال") — **لمندوب المشتريات/المطاعم بس** (`POST /orders/custom-delivery`)، لو اخترتها لازم كمان
  تبعت:
  - `transferType` (إجباري): `BANK_TRANSFER` | `VODAFONE_CASH` | `INSTAPAY` | `OTHER`.
  - `transferImage` (إجباري): صورة إيصال التحويل (ملف، مش `imageIds`).
  - لو `transferType = BANK_TRANSFER` → `transferAccountNumber` (رقم حساب/آيبان) إجباري.
  - لو أي نوع تاني → `transferNumber` (رقم الهاتف اللي حوّل منه) إجباري، **ولازم يكون رقم مصري صحيح** (بصيغة
    `01[0/1/2/5]xxxxxxxx`، ١١ رقم) وإلا هيرجع 400 برسالة "رقم الهاتف غير صالح...".
  - **مندوب الأونلاين (`POST /orders/online-delivery`) مالوش الحقول دي خالص** — لو حاولت تبعتها هترفض
    (`property ... should not exist`). يعني لو عندك شاشة دفع مشتركة، اخفي خيار "تحويل بإيصال" تمامًا لما يكون
    الأوردر أونلاين.

### `paidWithWallet` (اختياري، `boolean`) — ده الدفع الحقيقي من رصيد محفظة التطبيق
مستقل تمامًا عن `paymentMethod` — لو `true`، المبلغ الكلي بينخصم فورًا من رصيد محفظة العميل داخل التطبيق (لو
الرصيد مش كافي، الطلب بيترفض بـ"Insufficient balance"). متاح لكل الأنواع التلاتة (Purchase/Restaurant/Online).
ينفع تستخدمه مع `paymentMethod: CASH` كمان (يعني "ادفع من المحفظة" كخيار مستقل، مش شرط يترتبط بـ`WALLET`).

---

## ٤. بيانات الطلب اللي بترجع للمندوب والأدمن (كانت ناقصة، اتصلحت)

قبل كده، تفاصيل الأوردر في **داشبورد الأدمن لكل مندوب** (`GET /delivery/:id/dashboard`) كانت بترجع فاضية للمندوب الخاص بكل أنواعه التلاتة — مفيش منطقة، مفيش محطات. اتصلحت: دلوقتي كل أوردر (Purchase/Restaurant/Online) بيرجع فيه:

```json
{
  "id": 110,
  "customDeliveryKind": "ONLINE",
  "zoneId": 3,
  "zoneName": { "ar": "منطقة 3", "en": "Zone 3" },
  "stations": [
    {
      "sequence": 1,
      "type": "PICKUP",
      "name": "Nadys Online Shop",
      "lat": 30.045,
      "lng": 31.235,
      "zoneId": 1,
      "zoneName": { "ar": "وسط القاهرة", "en": "Central Cairo" },
      "addressDetails": null,
      "contactPhone": "01012345678"
    },
    {
      "sequence": 2,
      "type": "DROPOFF",
      "name": "Sara",
      "lat": 26.4399,
      "lng": 50.108,
      "zoneId": 2,
      "zoneName": { "ar": "منطقة 2", "en": "Zone 2" },
      "addressDetails": "شارع النصر، عمارة 12",
      "contactPhone": "01098765432"
    }
  ]
}
```
**تحديث**: اكتشفنا كمان إن `GET /orders` / `GET /orders/:id` (نفس الاندبوينت اللي بيستخدمه الموبايل وصفحة تفاصيل الأوردر في الداشبورد) كان مش بيرجّع `zoneId` بتاع الأوردر خالص، ولا اسم المنطقة (`Zone.name`) لكل محطة — كان بيرجع بس رقم `zoneId` الخام على كل محطة من غير اسمها. اتصلح بردو: دلوقتي كل رد من `GET /orders`/`GET /orders/:id` فيه:
- `zoneId` على مستوى الأوردر نفسه (المنطقة الفعلية المحسوب عليها السعر).
- `Zone: { name: { ar, en } }` على مستوى الأوردر.
- `Zone: { name: { ar, en } }` جوه كل عنصر في `Stations[]` (بالإضافة لـ`zoneId` الخام اللي كان موجود بالفعل).

يعني دلوقتي الاندبوينت ده (اللي بيستخدمه الموبايل لصفحة تفاصيل الطلب) بيرجّع اسم المنطقة كامل، مش رقم بس.

**تحديث تاني**: `GET /delivery/:id/dashboard` نفسه كان بيجيب `customDeliveryKind` من قاعدة البيانات بس بيسقطه قبل ما يرجعه في الرد (باگ في الـmapping، مش في الاستعلام) — يعني الداشبورد ماكانش فيه أي وسيلة يعرف الأوردر ده Purchase ولا Restaurant ولا Online. اتصلح: `customDeliveryKind` بيرجع دلوقتي في كل عنصر من `orders[]`.

---

## اتأكدنا منه حي

- إنشاء طلب `RESTAURANT` بمحطتين زون بس (من غير أي `lat`/`lng`) — رجع `zoneId` صح على الأوردر والشحن اتحسب من سعر المنطقة.
- إزالة سعر المنطقة والتأكد إن الحساب رجع تلقائي للكيلومتر.
- إنشاء طلب `ONLINE` بمرسل واحد ومستلمين مختلفين — كل مستلم رجع بمنطقته وعنوانه صح في محطات الأوردر.
- تعيين الطلبين لمندوب، قبول المندوب لهم، والتأكد إن داشبورد الأدمن (`/delivery/:id/dashboard`) رجّع المنطقة والمحطات كاملة لكل الحالتين.
- **حساب الكيلومترات (fallback)**: جربنا محطتين بإحداثيات حقيقية جوه منطقة مُعرّفة (من غير أي سعر منطقة)، وحسبنا المسافة يدويًا (هافرساين) بين النقطتين، وقارناها بالرقم اللي رجع من `POST /orders/custom-delivery/calculate` — طابق تمامًا (٤.٤١ كم × رسم الكيلومتر + الرسم الأساسي من الإعدادات). نفس الشيء اتأكد منه لإنشاء أوردر حقيقي ووصول نفس الرقم للمندوب. يعني حساب المسافة الحقيقي شغال صح ومش عدد وهمي أو مكسور.
- **تسعير الأونلاين بالخريطة (جديد)**: جربنا ٥ حالات حي — (١) مفيش خريطة ولا سعر منطقة → رسم أساسي ثابت. (٢) سعر منطقة محدد + خريطة مستخدمة كمان → سعر المنطقة بياخد الأولوية زي المتوقع. (٣) مفيش سعر منطقة، خريطة مستخدمة على الاتنين (البائع والمستلم) → السعر اتحسب بالمسافة الحقيقية وطابق الحساب اليدوي تمامًا. (٤) خريطة مستخدمة من طرف واحد بس → رجع للرسم الثابت، مش حساب مسافة غلط. (٥) مستلم ثاني استخدم الخريطة برضو → سعره اتحسب برسم المحطة الإضافية الثابت بس، مش بمسافته هو. كل الحالات دي اتأكد منها بأوردر حقيقي ووصول نفس السعر للمندوب.
- نضّفنا بيانات الاختبار بعد كده.

---

## ✅ ملخص كل حاجة مطلوبة من الفرونت (Checklist)

1. **محطات المشتريات/المطاعم**: `zoneId` إجباري، `lat`/`lng` اختياريين (زرار خريطة اختياري)، **مفيش خانة اسم/رقم
   مستلم في نموذج المحطة خالص** — شيلها لو موجودة.
2. **أي محطة غير الأخيرة تتعامل كاستلام** بغض النظر عن ترتيب إضافتها — لو عندك مشكلة عرض تسميات غلط لمحطة
   جديدة، دي مشكلة فرونت (شوف القسم بتاعها فوق).
3. **الأونلاين**: زرار خريطة اختياري جنب عنوان البائع وجنب عنوان كل مستلم — استخدامه بيأثر على سعر أول مستلم بس
   (مش الباقيين).
4. **رفع الصور**: نفس اندبوينت الرفع (`POST /orders/custom-delivery/images`) وحقل `imageIds` شغالين دلوقتي
   للتلاتة أنواع (مشتريات/مطاعم/أونلاين) — لو الواجهة كانت مخفية زرار الصور للأونلاين، فعّلها.
5. **شاشة الدفع**: فرّق بين `paymentMethod: WALLET` (تحويل بإيصال، Purchase/Restaurant بس) و`paidWithWallet`
   (خصم من محفظة التطبيق، متاح للتلاتة) — دول حقلين مختلفين تمامًا، شوف قسم "طريقة الدفع" فوق بالتفصيل.
6. **زون التوصيل للأوردر العادي**: لو عندك دروب منيو لاختيار منطقة التوصيل في شاشة أوردر المطعم/المتجر العادي،
   استخدم `GET /stores/:id/effective-zone-prices` لعرض السعر الصح لكل منطقة **الخاص بالمتجر ده تحديدًا**، مش
   `GET /zones` العادي.
7. **معاينة السعر قبل تسجيل الدخول**: `POST /orders/custom-delivery/calculate` بس هو اللي متاح كزائر — أي حاجة
   تانية (خصوصًا كل اندبوينتس الأونلاين) محتاجة تسجيل دخول من الأول.
