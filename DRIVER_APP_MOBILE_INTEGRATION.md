# تطبيق المندوب (Driver App) — كل الجديد اللي محتاجه الموبايل ديفلوبر

> الهدف من الملف ده: كل حاجة اتضافت أو اتصلحت في بيانات المندوب النهارده، موثقة هنا بالتفصيل —
> اندبوينت، شكل الرد، وأمثلة حقيقية اتجرّبت حي. مفيش حاجة هنا نظرية.

## نظرة عامة على الاندبوينتس اللي بيستخدمها تطبيق المندوب

**كل الاندبوينتس دي محتاجة تسجيل دخول (Auth) — مفيش أي حاجة منها متاحة كزائر.**

| الاندبوينت | Method | الاستخدام |
|---|---|---|
| `GET /delivery/me/pending-assignments` | GET | العروض الجديدة المعلقة (لسه ما اتقبلتش) |
| `PATCH /orders/:id/accept` | PATCH | قبول عرض **واحد بعينه** (بمعرّف الأوردر) |
| `PATCH /orders/:id/reject` | PATCH | رفض عرض **واحد بعينه** |
| `PATCH /delivery/me/assignments/accept` | PATCH | قبول **كل** العروض المعلقة دفعة واحدة (من غير body) |
| `GET /delivery/me/current-assignment` | GET | العرض/الأوردر النشط الحالي (لو المندوب رجع فاتح التطبيق) |
| `GET /orders` أو `GET /orders/:id` | GET | تفاصيل أي أوردر (تاريخ الأوردرات، أو أوردر محدد) |
| `PATCH /orders/custom-delivery/:id/advance` | PATCH | "انتقل للمحطة التالية" (مندوب خاص بس) |
| `PATCH /orders/custom-delivery/:id/finish` | PATCH | "إنهاء المهمة" (المحطة الأخيرة) |
| `GET /delivery/me/wallet` | GET | ملخص المحفظة (٣ أرقام) |
| `POST /delivery/me/withdraw` | POST | طلب سحب |
| `GET /delivery/me/withdrawals` | GET | قائمة طلبات السحب |

### إيه اللي بيحصل بالظبط لما المندوب يقبل أوردر — حالة المحطات (Station Status)

كل محطة (`OrderStation`) بتعدي بـ٣ حالات: `WAITING` → `GOING` → `REACHED`.

1. لما المندوب يقبل الأوردر (`accept` بأي طريقة من التلاتة فوق)، **أول محطة بتتحول تلقائي لـ`GOING`** —
   مفيش أي اندبوينت تاني تحتاجه عشان "يبدأ" أول محطة، القبول نفسه كافي.
2. `PATCH /orders/custom-delivery/:id/advance` بياخد المحطة الحالية (اللي حالتها `GOING`) ويخليها `REACHED`،
   وياخد اللي بعدها ويخليها `GOING`. **لازم يبقى فيه محطة حالتها `GOING` فعلًا وإلا هيرجع 400** ("No active
   station to complete").
3. **`advance` مينفعش يتستخدم للمحطة الأخيرة** — هيرجع 400 ("Last station must be completed via finish").
   المحطة الأخيرة لازم تتقفل بـ`finish` بس.
4. `PATCH /orders/custom-delivery/:id/finish` بيتأكد إن كل المحطات اللي قبل الأخيرة بقت `REACHED`، بعدين يقفل
   الأخيرة ويحوّل الأوردر لـ`DELIVERED` كله في نفس الوقت.

---

## ١. المنطقة والعنوان — بيرجعوا كاملين دلوقتي في كل أوردر

قبل كده كان `zoneId`/اسم المنطقة مش بيرجع خالص في تفاصيل الأوردر (`GET /orders`, `GET /orders/:id`)، ولا في محطات
المندوب الخاص. اتصلح، ودلوقتي كل أوردر بيرجع فيه:

```json
{
  "zoneId": 2,
  "Zone": { "name": { "ar": "منطقة 2", "en": "Zone 2" } },
  "Address": {
    "id": 1, "lat": 30.048, "lng": 31.238,
    "adress": "5 Dokki St, Giza, Cairo", "title": "Home", "default": true
  }
}
```

- `zoneId`/`Zone` = المنطقة **الفعلية** اللي اتحسب عليها السعر (مش بالضرورة اللي اختارها العميل من الدروب
  منيو — لو العميل اختار منطقة، هي بتاخد الأولوية في التسعير لكن الحقل ده بيرجع المنطقة الفعلية المستخدمة).
- `Address` بترجع بس للأوردرات العادية (مش المندوب الخاص، ده بيستخدم `Stations` بدالها — شوف تحت).

---

## ٢. تفاصيل الأوردر كاملة — نفس اللي شافه العميل بالظبط

`OrderItems` في رد `GET /orders/:id` دلوقتي فيه كل حاجة: المقاس، الإضافات، **العرض (bundle) لو فيه**، وسعر
المنتج قبل/بعد الخصم — واتأكد حي إن كل رقم مطابق ١٠٠٪ لنفس الأوردر لما شافه العميل وقت الطلب.

```json
{
  "OrderItems": [
    {
      "price": 90,
      "isFree": false,
      "quantity": 1,
      "Size": { "id": 2, "name": { "ar": "كبير", "en": "Large" }, "price": 90 },
      "OrderItemAddons": [
        { "Addon": { "id": 1, "name": { "ar": "جبن إضافي", "en": "Extra Cheese" }, "price": 15 } }
      ],
      "Service": {
        "id": 1,
        "name": { "ar": "برجر كلاسيك", "en": "Classic Burger" },
        "image": "uploads/services/burger.png",
        "price": 75,
        "priceAfterDiscount": 60
      },
      "OrderBundle": null
    }
  ]
}
```

### لو المنتج ده جزء من عرض (bundle، زي "اشتري ٢ واحصل على ١")

`OrderBundle` بترجع مليانة بدل `null`:

```json
{
  "price": 0,
  "isFree": true,
  "OrderBundle": {
    "title": { "ar": "اشتري ٢ واحصل على ١", "en": "Buy 2 Get 1" },
    "type": "BUY_X_GET_Y_FREE",
    "requiredPaidQuantity": 2,
    "freeQuantity": 1,
    "freeDiscountAmount": 60
  }
}
```

`isFree: true` يعني القطعة دي هدية — `price` بتاعها بيبقى صفر دايمًا. اعرض بادچ "هدية" أو زي كده على القطعة دي
في واجهة المندوب عشان يعرف يوصّلها مع الأوردر من غير ما يفتكرها غلط في الفاتورة.

**ملاحظة**: صور المنتج (`Service.image`) صورة واحدة بس لكل منتج — مفيش معرض صور متعدد للمنتجات في النظام
حاليًا.

---

## ٣. المندوب الخاص بأنواعه التلاتة (Purchase / Restaurant / Online)

كل الأنواع بتستخدم نفس الـ`Stations[]` (محطة استلام + محطة أو أكتر تسليم)، وكل محطة بترجع:

```json
{
  "sequence": 1,
  "type": "PICKUP",
  "name": "Nadys Kitchen",
  "lat": 30.045,
  "lng": 31.235,
  "zoneId": 1,
  "Zone": { "name": { "ar": "وسط القاهرة", "en": "Central Cairo" } },
  "contactPhone": "01012345678",
  "addressDetails": null,
  "purchaseList": null,
  "estimatedCost": 0,
  "collectionAmount": null,
  "packagingRequested": false,
  "status": "WAITING",
  "Images": []
}
```

- `customDeliveryKind` على مستوى الأوردر بيقولك النوع (`PURCHASE`/`RESTAURANT`/`ONLINE`) — استخدمه بس لتغيير
  التسميات في الواجهة (محطة/مطعم، قائمة مشتريات/قائمة أوردر) — **المنطق والتعامل مع الأوردر واحد للتلاتة**.
- **الأونلاين تحديدًا**: محطة PICKUP واحدة (بيانات المرسل/البائع) + محطة DROPOFF لكل مستلم — كل مستلم بمنطقته
  وعنوانه وتليفونه وقائمة أوردره الخاصة بيه (`purchaseList`)، منفصلين تمامًا عن بعض.
- **⚠️ مندوب المشتريات/المطاعم مفيهوش بيانات "مستلم" خالص** — كل محطة (استلام أو تسليم) هي بس مكان + قائمة
  اللي عايزه العميل منه، مفيش اسم/رقم مستلم في أي محطة من محطاتهم. الاستثناء الوحيد اللي فيه بيانات مستلم حقيقية
  هو الأونلاين بس (لأنه بائع واحد بيبعت لمستلمين مختلفين، مفهوم مختلف تمامًا).
- **كل محطة غير الأخيرة = استلام تلقائيًا** — الباك إند بيحدد النوع (Pickup/Dropoff) حسب ترتيب المحطة في
  القايمة (آخر واحدة بس = تسليم)، مش حسب وقت إضافتها. لو ظاهر عندك تسميات "تسليم" لمحطة لسه مضافة في النص، دي
  مشكلة عرض في الفرونت مش نقص بيانات من الباك إند.
- **الموقع (`lat`/`lng`) اختياري من العميل لكل الأنواع التلاتة** — لو مبعتش، الباك إند بيحسبه تلقائي من مركز
  المنطقة. يعني ممكن تلاقي إحداثيات مش دقيقة ١٠٠٪ لموقع حقيقي على الخريطة لو المستخدم مستخدمش زرار الخريطة —
  لو عايز تعرض بينة، اعتبرها تقريبية مش موقع GPS حقيقي في الحالة دي.
- **الصور (`Images`) دلوقتي شغالة للتلاتة أنواع** — كانت لحد وقت قريب مقصورة على Purchase/Restaurant بس
  (الأونلاين كان بيرفض `imageIds` تمامًا بـ400)، اتصلحت واتأكد منها حي: كل مستلم في الأونلاين ممكن يبقى له
  صوره الخاصة بيه (نفس اندبوينت الرفع `POST /orders/custom-delivery/images`)، وكل مستلم بياخد صوره بس من غير
  اختلاط مع مستلم تاني في نفس الطلب.
- **⚠️ مهم — `collectionAmount`/`addressDetails`/`packagingRequested` مش لكل الأنواع**: الحقول التلاتة دي
  بترجع دايمًا `null`/`false` لمحطات **Purchase وRestaurant** — العميل مش بيقدر يحددهم أصلًا لما بيعمل الطلب
  (مش موجودين في نموذج إدخال محطة المشتريات/المطاعم، بس موجودين في نموذج مستلم الأونلاين). يعني في واجهة
  المندوب: لو الأوردر Purchase/Restaurant، متعرضش خانة "قيمة تحصيل" أو "تغليف" أصلًا (هتبقى فاضية دايمًا) —
  دي خاصة بالأونلاين بس. اتأكد من الكلام ده حي.

### التنقل بين المحطات (Purchase/Restaurant/Online — كلهم بنفس الطريقة)

- `PATCH /orders/custom-delivery/:id/advance` — كل ما المندوب يوصل محطة وينهيها، ينده عليه ينتقل للمحطة اللي
  بعدها.
- `PATCH /orders/custom-delivery/:id/finish` — لما يوصل آخر محطة وينهي المهمة بالكامل. **لازم كل المحطات اللي
  قبلها تكون في حالة REACHED قبل ما ده ينجح، وإلا هيرجع 400.**

### ⚠️ `lat`/`lng` بقوا إجباريين على `advance`/`finish` — وفيه تحقق فعلي من الموقع دلوقتي

كان الأوردر العادي (توصيل من متجر) عنده تحقق فعلي إن المندوب موجود فعليًا عند الفرع/عند العميل قبل ما تقبل حركة
"في الطريق"/"تم التسليم" — لكن ده كان **مش شغال خالص لمندوب المشتريات/المطاعم/الأونلاين**، الـ`lat`/`lng` كانوا
بس بيتسجلوا للعرض على الخريطة من غير أي تحقق حقيقي. اتصلح دلوقتي:

- **`lat`/`lng` بقوا إجباريين في الـbody لكل من `advance` و`finish`** (كانوا اختياريين قبل كده) — لو متبعتش، هترجع
  400 مباشرة من التحقق (`"lat must be a number..."`).
- **لازم موقع المندوب (اللي بتبعته) يقع جغرافيًا جوه نفس منطقة المحطة اللي بيقفلها** (منطقة الاستلام لو `advance`
  على محطة استلام، منطقة التسليم لو آخر محطة عبر `finish`) — التحقق ده بيقارن منطقة المندوب الفعلية (محسوبة من
  الـ`lat`/`lng` اللي بعتها) بمنطقة المحطة، مش مجرد مسافة ثابتة بالمتر — وده مقصود عشان المناطق بتاعتنا مساحات
  كبيرة، مش نقطة واحدة.
- لو المندوب برة منطقة المحطة، هيرجع 400 برسالة **"يجب أن تكون داخل منطقة الاستلام لإتمام هذه الخطوة"** (أو
  "التسليم" لو آخر محطة).

**التوصية للواجهة**: ابعت أحدث إحداثيات GPS حقيقية للمندوب مع كل نداء `advance`/`finish` (مش قيمة وهمية أو
قديمة) — وإلا هيترفض الطلب. لو المندوب فعلاً واقف في مكان التسليم/الاستلام لكن برضو بيترفض، يبقى في مشكلة GPS أو
بيانات منطقة غير دقيقة تستاهل تتبلغ عنها.

### أخطاء ممكن تواجهها (رسايل الـ400 بالظبط)

| الاندبوينت | الحالة | الرسالة |
|---|---|---|
| `accept` (فردي أو جماعي) | مفيش عرض معلق أصلًا أو خلص وقته | `"No pending assignment found or it has expired"` |
| `accept` | العرض خلص وقته فعليًا (بيتحدد كـ`TIMEOUT` وقتها) | `"Assignment has expired"` |
| `accept` | الأوردر اتلغى/اترفض من المتجر قبل ما المندوب يرد | `"This order is no longer available"` |
| `reject` | مفيش عرض معلق بنفس المعرّف | `"No pending assignment found"` |
| `advance` | مفيش محطة حالتها `GOING` دلوقتي | `"No active station to complete"` |
| `advance` | اتنادى على المحطة الأخيرة (لازم `finish` بدالها) | `"Last station must be completed via finish"` |
| `advance`/`finish` | مفيش `lat`/`lng` في الـbody | `"lat must be a number conforming to the specified constraints,lng must be a number..."` |
| `advance`/`finish` | موقع المندوب برة منطقة المحطة | `"يجب أن تكون داخل منطقة الاستلام/التسليم لإتمام هذه الخطوة"` |
| `finish` | فيه محطة قبل الأخيرة لسه مش `REACHED` | `"Cannot finish before completing all stations"` |

### التسعير — إزاي بيتحدد سعر التوصيل

- **Purchase/Restaurant**: لو آخر محطة (التسليم) في منطقة عندها سعر توصيل ثابت محدد من الأدمن، يتاخد هو. لو
  مفيش، يتحسب بالمسافة الفعلية بالكيلومتر بين كل المحطات (مش خط مستقيم وهمي — بيحاول ياخد مسافة طريق حقيقية،
  ولو فشل بيرجع لحساب هافرساين). **حاليًا (بداية الإطلاق) كل المناطق من غير سعر محدد عن قصد** — يعني عمليًا
  التسعير هيشتغل بالكيلومتر من الإعدادات لحد ما يتقرر تفعيل أسعار مناطق معينة لاحقًا.
- **Online**: أول مستلم في القايمة بياخد سعره بالترتيب ده: (١) سعر منطقته لو محدد، (٢) لو لأ وكل من البائع
  وأول مستلم استخدموا زرار الخريطة الاختياري (`pickupLat`/`pickupLng` و`lat`/`lng`)، السعر يتحسب بالمسافة
  الحقيقية بالكيلومتر بينهم (نفس فورمولا Purchase/Restaurant بالظبط) — دي إضافة جديدة، **كانت مش موجودة خالص
  قبل كده**. (٣) لو ولا ده ولا ده، رسم أساسي ثابت من الإعدادات. أي مستلم زيادة بعد الأول بياخد رسم "محطة إضافية"
  ثابت دايمًا، بغض النظر عن منطقته أو خريطته هو.
- **مهم**: أي سعر بيتحسب وقت إنشاء الأوردر بيتسجل في `shipping` وبيفضل ثابت — نفس الرقم اللي هيشوفه المندوب هو
  نفسه اللي اتحسب وقت الطلب بالظبط (اتأكد منه حي بعدة سيناريوهات مختلفة).

---

## ⚠️ تسجيل الدخول والتحقق (`verified`) — اتغير السلوك بالكامل، شيل شاشة الـOTP

### قبل كده
مندوب لسه مش اتحقق منه (`verified: false`) كان تسجيل الدخول بيترفض له تمامًا (400/412 بتاعة `NOT_VERIFIED`)،
والتطبيق كان لازم يعرض شاشة "أدخل كود التحقق اللي جالك على الإيميل" (OTP) عشان يقدر يكمل الدخول.

### دلوقتي
**المندوب بيسجل دخول عادي جدًا حتى لو `verified: false`** — مفيش أي OTP أو كود مطلوب منه خالص عشان يفتح
التطبيق. الباك إند **مبيرجعش `NOT_VERIFIED` للمندوب أبدًا بعد كده** — أي كود فرونت بيستنى الرد ده أو بيوديه
لشاشة الـOTP بقى ميت (dead code)، مش هيتنفذ أبدًا.

`verified` بقى بس **علامة هل الأدمن اعتمد المندوب ده ولا لسه** — ورد ده بيرجع في بيانات اليوزر بعد تسجيل الدخول
(`"verified": false`). أثره الوحيد: **استقبال الأوردرات**، مش تسجيل الدخول.

### الشروط الثلاثة عشان المندوب يستقبل أي أوردر (لازم الثلاثة مع بعض)

| # | الشرط | مين بيتحكم فيه |
|---|---|---|
| ١ | `verified: true` | **الأدمن بس** من الداشبورد (`PATCH /delivery/:id`) — مفيش أي فعل من المندوب أو OTP يفعّله |
| ٢ | `active: true` | الأدمن (تفعيل الحساب) |
| ٣ | `availableNow: true` | مواعيد عمل المندوب، أو تفعيل "متاح إجباري" (`forceAvailable`) |

لو أي شرط ناقص، المندوب بيفتح التطبيق ويستخدمه عادي، لكن مش هيوصله أي عرض أوردر جديد.

### المطلوب بالظبط من الفرونت (خطوات)

1. **شيل شاشة "أدخل كود التحقق" (OTP) من مسار تسجيل دخول المندوب بالكامل** — الباك إند مش هيطلبها أبدًا،
   فالشاشة دي بقت زيادة وممكن تتعارض مع أي منطق تاني لو فضلت موجودة.
2. لو الكود بيتشيك على response معين زي `precondition failed` / `NOT_VERIFIED` / كود ٤١٢ في مسار لوجن
   المندوب تحديدًا — امسحه، مش هيحصل تاني.
3. **اعرض بادچ/رسالة "لسه في انتظار اعتماد الأدمن"** في البروفايل أو الصفحة الرئيسية لو `verified: false` —
   عشان المندوب يفهم ليه مش بيستقبل أوردرات من غير ما يستغرب.
4. باقي مسار تسجيل الدخول (إيميل + باسورد) **زي ما هو بالظبط، من غير أي تعديل تاني**.

### التحقق من الموقع (advance/finish) — أساسه مختلف عن الأوردر العادي

| النوع | التحقق بيتم إزاي |
|---|---|
| **الأوردر العادي** (توصيل من متجر) | **مسافة بالمتر** من فرع المتجر (وقت الاستلام) أو عنوان العميل المحفوظ (وقت التسليم) — نفس الآلية القديمة، متغيرتش |
| **مندوب المشتريات/المطاعم/الأونلاين** | **عضوية المنطقة (Zone)** — هل موقع المندوب واقع جغرافيًا جوه نفس منطقة المحطة، مش مسافة بالمتر (شوف تفاصيل القسم فوق) |

الفرق مقصود: مناطقنا مساحات كبيرة، فمسافة ثابتة بالمتر كانت هترفض مندوبين موجودين فعليًا في المنطقة بس بعيدين
عن مركزها.

**اتأكدنا منه حي**: مندوب جديد اتسجل دلوقتي (`verified: false`) قدر يسجل دخول ويرجع Token كامل من غير أي كود.
دور تاني (عميل) لسه شغال بنفس الطريقة القديمة (اترفض لحد ما يتحقق OTP) — التغيير خاص بالمندوب بس.

---

## ٤. المحفظة — ٣ أرقام منفصلة + السحب

`GET /delivery/me/wallet`:

```json
{
  "total": 207.47,
  "commission": 20,
  "delivery": 52.59,
  "pendingWithdraw": 0,
  "totalWithdrawn": 0
}
```

| الحقل | يعني إيه |
|---|---|
| `delivery` | **فلوسه هو** — إجمالي رسوم التوصيل اللي كسبها من كل الأوردرات (بغض النظر عن نوعها: عادي/مشتريات/مطاعم/أونلاين). ده اللي بيتسحب منه (شوف تحت). |
| `commission` | **اللي عليه للمنصة** — عمولة المنصة المتجمعة من كل الأوردرات كمان (مجمّعة من الأنواع الأربعة مع بعض في رقم واحد، مش منفصلين). بتتصفّر بس لما الأدمن يعمل تسوية كاش يدوية. |
| `total` | **اللي في جيبه** — إجمالي الكاش اللي حصّله من العملاء (أوردرات الدفع كاش بس). ده مش فلوسه، ده أمانة محتاج يسلّمها/تتحاسب معاه فيها. |
| `pendingWithdraw` | مجموع طلبات السحب المعلقة حاليًا. |
| `totalWithdrawn` | إجمالي اللي اتسحب فعليًا وقبله الأدمن. |

**اتأكد حي**: عملنا أوردر من كل نوع (عادي، مشتريات، أونلاين) بعمولة حقيقية، وتابعنا الأرقام التلاتة بعد كل
تسليم — بتتحدث صح ومجمّعة مع بعض، مش منفصلة بنوع الأوردر.

### طلب سحب

```
POST /delivery/me/withdraw
{ "amount": 10, "payoutMethod": "VODAFONE_CASH", "payoutDetails": "01012345678" }
```

`payoutMethod`: `CASH` | `VODAFONE_CASH` | `INSTAPAY`. الطلب بيتسجل `PENDING` — الأدمن هو اللي يوافق/يرفض من
لوحته. لما يوافق، المبلغ بينخصم من `delivery` (فلوسه) وبينقل من `pendingWithdraw` لـ`totalWithdrawn` — **مبينخصمش
من `total`/الكاش المحصّل خالص**، لأنهم رصيدين منفصلين تمامًا.

```
GET /delivery/me/withdrawals
```
بترجع قائمة كل طلبات السحب بتاعة المندوب بحالتها (`PENDING`/`APPROVED`/`DENIED`).

---

## اتأكدنا منه حي (مش قراءة كود بس) — عشان الثقة تبقى مبنية على تجربة فعلية

- كل رقم تسعير (زون محدد / سعر عام / fallback بالكيلومتر) لكل الأنواع الأربعة (عادي، مشتريات، مطاعم، أونلاين) —
  اتقارن بين رد إنشاء الأوردر ورد المندوب، وطابق تمامًا في كل الحالات.
- حساب المسافة بالكيلومتر (fallback) — اتحسب يدويًا (هافرساين) وقورن بالرقم الراجع من الـAPI، طابق تمامًا.
- تفاصيل المنتج (مقاس/إضافة/عرض/صورة/سعر قبل وبعد الخصم) — اتقارنت بين رد العميل ورد المندوب لنفس الأوردر، حرفيًا مطابقة.
- قبول/رفض عرض واحد بعينه (`PATCH /orders/:id/accept`, `/reject`) — جُرّبوا فعليًا (مش بس قراءة كود)، اشتغلوا صح.
- قبول كل العروض المعلقة دفعة واحدة (`PATCH /delivery/me/assignments/accept`) — جُرّب فعليًا.
- `GET /delivery/me/current-assignment` — جُرّب فعليًا، رجّع الأوردر النشط بمنطقته صح.
- التنقل بين المحطات (`advance`/`finish`) — جُرّب فعليًا لأوردر Purchase وأوردر Online.
- المحفظة الـ٣ أرقام + السحب (طلب وموافقة أدمن) — جُرّب فعليًا لكل الأنواع الأربعة، بما فيهم تجميع العمولة عبر الأنواع في رقم واحد.
- **التحقق من موقع المندوب عند `advance`/`finish` (جديد)**: جربنا محطتين في منطقتين بعيدتين عن بعض — `advance`
  بموقع غلط (المندوب في منطقة التسليم بدل الاستلام) اترفض صح، بموقع صح نجح. نفس الاختبار لـ`finish`. وجربنا
  الطلب من غير `lat`/`lng` خالص، اترفض من التحقق قبل ما يوصل لأي منطق تاني.

## ⚠️ صور المحطات (والصور بشكل عام) — رفع الصورة شغال، لكن عرضها لسه محتاج تأكيد

جربنا رفع صورتين حقيقيتين لمحطة (`POST /orders/custom-delivery/images`) وعمل أوردر بيهم — والمسار كامل شغال
صح: الملفات اتحفظت على السيرفر، الأوردر اترجع فيه `Stations[].Images` بالمسار الصحيح، ونفس البيانات ظهرت
مطابقة عند العميل والأدمن والمندوب.

**لكن** لما جربنا نفتح رابط الصورة نفسه (`http://<server>/uploads/orders/...png`) على السيرفر المحلي، رجع
**404** — لقينا إن مفيش أي إعداد لتقديم ملفات `uploads/` كملفات ثابتة (static) في كود الباك إند خالص (ولا في
`main.ts`, ولا `nginx`/reverse-proxy جوه `docker-compose` بتاع المشروع). يعني حاليًا، في هذه البيئة بالذات، مفيش
طريقة يوصل بيها أي حد لصورة اترفعت عن طريق السيرفر ده — **مش مشكلة خاصة بصور المحطات بس، دي بتأثر على أي صورة في
النظام كله** (صورة منتج، شعار متجر، صورة تحويل بنكي، إلخ) لو اترفعت محليًا.

**الاحتمالين الممكنين**:
1. لو عندكم في الإنتاج سيرفر تاني قدام الـNode app (زي Nginx أو CDN) بيقدّم فولدر `uploads/` كملفات ثابتة —
   ساعتها الموضوع طبيعي وشغال في الإنتاج رغم إنه مش شغال محليًا عندي دلوقتي.
2. لو مفيش زي كده — فالصور مش هتتفتح فعليًا في أي مكان، مش بس تطبيق المندوب.

**التوصية**: افتح أي رابط صورة موجود بالفعل من التطبيق الحالي (مثلاً صورة منتج من الداشبورد أو الموبايل) في
براوزر عادي وشوف هل بيفتح ولا لأ. لو بيفتح، يبقى مفيش مشكلة والموضوع محلول من جهة السيرفر بره الكود. لو مبيفتحش،
يبقى دي مشكلة أعمق من صور المحطات وتستاهل نتكلم فيها لوحدها.

---

## ملاحظة عامة

كل الحقول الجديدة اللي فوق **إضافية على الرد الحالي** — مفيش حقل اتشال أو اتغير شكله. أي كود موبايل حالي هيفضل
شغال زي ما هو، والتعديلات دي بس بتضيف بيانات كانت ناقصة قبل كده.

---

## ✅ ملخص كل حاجة مطلوبة من الفرونت (Checklist)

1. **اعرض المنطقة واسمها** (`zoneId`/`Zone.name`) في تفاصيل أي أوردر — كان مفقود خالص، اتصلح.
2. **اعرض تفاصيل العرض/الباندل** (`OrderBundle`) لو موجود على أي صنف — بادچ "هدية" للصنف اللي `isFree: true`.
3. **اعرض سعر المنتج قبل وبعد الخصم** (`Service.price`/`priceAfterDiscount`) جنب سعر السطر النهائي.
4. **استخدم `PATCH /orders/:id/accept`/`reject` للقبول/الرفض الفردي** (زر لكل عرض على حدة)، أو
   `PATCH /delivery/me/assignments/accept` لو محتاج زرار "اقبل الكل" — الاتنين شغالين ومجربين.
5. **بعد القبول، أول محطة بتتفعّل تلقائي (`GOING`)** — مفيش زرار "ابدأ" منفصل مطلوب.
6. **زرار "التالي" (`advance`) يظهر بس لو المحطة الحالية مش آخر واحدة** — آخر محطة ليها زرار "إنهاء" (`finish`)
   بس، مش "التالي".
7. **اعرض رسايل الأخطاء المكتوبة فوق** (مش رسالة عامة "حصل خطأ") لكل حالة من حالات accept/reject/advance/finish.
8. **شاشة المحفظة**: ٣ أرقام منفصلة (`delivery`=فلوسه، `commission`=عليه، `total`=في جيبه) — متجمعهمش في رقم
   واحد. السحب بيتم من `delivery` بس.
9. **صور المحطات دلوقتي شغالة للتلاتة أنواع** (كانت الأونلاين ناقصة، اتصلحت) — لو الواجهة مخفية زرار الصور
   للأونلاين، فعّلها.
10. **افتح رابط صورة حقيقية موجودة بالفعل في التطبيق وتأكد إنها بتفتح** قبل ما تعتمد عليها في واجهة المندوب —
    شوف قسم "صور المحطات" فوق.
11. **⚠️ مهم جدًا**: `lat`/`lng` بقوا **إجباريين** في كل نداء `advance`/`finish` — لازم تبعت أحدث موقع GPS حقيقي
    للمندوب مع كل نداء، وإلا الطلب هيترفض. لو المندوب حاول يقفل محطة وهو فعلًا برة منطقتها هيترفض بردو برسالة
    واضحة — اعرضها للمندوب بدل رسالة خطأ عامة.
12. **⚠️ شيل شاشة "أدخل كود التحقق" من تسجيل دخول المندوب** — الباك إند مبيرجعش `NOT_VERIFIED` للمندوب خالص
    بعد كده، بيسجل دخول عادي دايمًا. اعرض بدالها بادچ "لسه في انتظار اعتماد الأدمن" لو `verified: false` في
    بروفايله.
